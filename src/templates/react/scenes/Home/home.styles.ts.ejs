import { getTheme, pxToRem, styled } from '~/utils';

const backgroundSecondary = getTheme('background.secondary');
const backgroundTertiary = getTheme('background.tertiary');
const backgroundFifth = getTheme('background.fifth');
const textPrimary = getTheme('text.primary');

export const Section = styled.div`
  width: 100%;
  height: 100vh;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  background-color: ${backgroundSecondary};
`;

export const Image = styled.img`
  width: ${pxToRem(200)};
  height: ${pxToRem(200)};
`;

export const Button = styled.button`
  border: 0;
  min-width: ${pxToRem(150)};
  height: ${pxToRem(40)};
  padding: 0px ${pxToRem(40)};
  border-radius: ${pxToRem(20)};
  background-color: ${backgroundTertiary};
  color: ${textPrimary};
  cursor: pointer;

  :hover {
    transition: 0.5s;
    background-color: ${backgroundFifth};
  }
`;
